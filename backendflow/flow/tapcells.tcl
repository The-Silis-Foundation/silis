{tap_cmd}
#tapcell -distance 14
{write_cmd}

# --- TAPCELL FLAGS ---
# tapcell:
#   -distance <microns> : Max distance between tap cells to prevent latch-up.
#   -tapcell_master <name> : Explicitly specify the tap cell name to use.
